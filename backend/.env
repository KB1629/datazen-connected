PORT=3001
# Try different NiFi URLs if you're having connection issues:
# Default HTTPS URL (secure):
NIFI_API_URL=https://localhost:8443/nifi-api
# Default HTTP URL (non-secure):
# NIFI_API_URL=http://localhost:8080/nifi-api
# For NiFi running on a different machine, use its IP or hostname:
# NIFI_API_URL=http://your-nifi-server:8080/nifi-api

# NiFi authentication credentials 
# This appears to be a certificate identity (Distinguished Name)
NIFI_USERNAME=b8892325-17ce-4feb-8e21-34893ff960b6
# Password is not needed for certificate auth, but keeping it for compatibility
NIFI_PASSWORD=+PFJCwF2ARJetaqwfHqD7XAXF9kKurWo
# Disable SSL certificate verification for self-signed certs
NIFI_SSL_VERIFY=false
# Authentication method: 'certificate' (default), 'password', or 'none'
NIFI_AUTH_METHOD=certificate

# Mock mode - set to false to use the real NiFi server
MOCK_NIFI_API=false

# JWT Secret for API authentication
JWT_SECRET=your-secure-jwt-secret

# Add your NiFi authentication details if needed

